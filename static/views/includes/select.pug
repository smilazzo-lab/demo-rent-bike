mixin select(validationsel,key,dizionario)
    select(id=validationsel data-bind=key)
        each i in dizionario
            option(value=i.codice) #{i.descrizione}
    <i class="fas fa-check-circle"></i>
    <i class="fas fa-exclamation-circle"></i>
    <small>Error message</small>